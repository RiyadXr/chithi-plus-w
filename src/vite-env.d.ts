// Fix: Commented out to resolve a "Cannot find type definition file" error.
// This is safe because the application does not currently use Vite-specific client APIs.
// /// <reference types="vite/client" />
